services:
  ai-service:
    build: .
    ports:
      - "8000:8000"    # REST API
      - "50051:50051"  # gRPC Server
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
        - PYTHONUNBUFFERED=1
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
